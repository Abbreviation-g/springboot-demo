dubbo:
  application:
    name: dubbo-nacos-demo-provider
    qos-enable: false
    enable-file-cache: false
  protocol:
    name: dubbo
    port: -1
  registry:
    address: nacos://127.0.0.1:8848
    group: demo
  #    username: nacos
  #    password: nacos
  provider:
    loadbalance: roundrobin
server:
  port: 8082

