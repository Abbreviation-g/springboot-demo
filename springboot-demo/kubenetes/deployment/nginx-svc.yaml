apiVersion: v1
kind: Service
metadata:
  name: nginx-svc
  namespace: namespace-0327
  labels:
    app: nginx-svc
spec:
  ports:
    - port: 80
      targetPort: 80
      name: web
  type: NodePort
  selector:
    app: nginx-deploy
