apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deploy-s2
  namespace: namespace-0327
  labels:
    app: nginx-deploy-s2
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: nginx-deploy-s2
        security: S2
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: topology.kubernetes.io/zone
                    operator: In
                    values: [ R ]
      containers:
        - name: nginx
          image: nginx
          imagePullPolicy: IfNotPresent
  selector:
    matchLabels:
      app: nginx-deploy-s2
